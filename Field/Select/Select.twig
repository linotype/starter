<div data-field-id="{{field.uid}}" data-key="{{field.key}}" id="{{field.id}}" class="linotype-field {{field.class}}" data-type="Select" data-controller="Select">
    <h1>{{title}}</h1>
    <select id="select--{{field.id}}" name="{{field.key}}" class="linotype-field-value">
        <option value="1">1</option>
        <option value="2">2</option>
    </select>
    <div class="desc"><i>{{help}}</i></div>
</div>