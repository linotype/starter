<div id="{{field.id}}" class="linotype-field {{field.class}}" data-id="{{field.uid}}" data-key="{{field.key}}" data-type="Select" data-controller="Select">
    <h1>{{title}}</h1>
    <select id="select--{{field.id}}" name="{{field.key}}" class="linotype-field-value">
        <option value="1">1</option>
        <option value="2">2</option>
    </select>
    <div class="desc"><i>{{help}}</i></div>
</div>